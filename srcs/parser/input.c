#include <stdio.h>
#include "nn.h"

#define MAX_CHAR_LINE 200

// void parse_input(char *filename, TData *training_data)
// {
//     FILE *file;
//     char line[MAX_CHAR_LINE];
//     char *res;

//     file = fopen(filename, "r");
//     res = fgets(&line, MAX_CHAR_LINE, file);
//     do
//     {
//         res = fgets(&line, MAX_CHAR_LINE, file);

//     } while (res != NULL);

// }
